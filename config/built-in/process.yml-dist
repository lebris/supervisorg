logicalGroups:
    applications:
        defaultView: true
        icon: desktop
        regex: "(?P<groupValue>.+)--(?P<process>.+)"
        # filters: [regex_hidden_processes]
    animals:
        icon: object-ungroup
        regex: "(.+)--(?P<groupValue>.+)-(?P<process>.+)"
        # filters: [regex_hidden_processes]
